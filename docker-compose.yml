# Docker Compose 主配置文件
# 使用 include 指令组合不同类型的服务

include:
    - docker-compose.infra.yml # 基础设施/中间件服务
    - docker-compose.app.yml # 应用服务
    - docker-compose.tool.yml # 工具服务

networks:
    default:
        name: local-env-network
        driver: bridge
